"use strict"


// Позначення

// Кількість кнопок - numberOfButtons


// Введення даних

const numberOfButtons = 8

// Обчислення результатів
if (confirm('Почати тестування?')) {
    let button
    for (let i = 1; i <= numberOfButtons; i++) {
        button = document.write(`<button>Hello</button>`)
    
    }
}
// Виведення результатів


